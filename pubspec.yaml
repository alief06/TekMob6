name: laporcepat
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  google_fonts: ^8.1.0
  image_picker: ^1.2.2
  intl: ^0.20.2
  path: ^1.9.1
  provider: ^6.1.5+1
  sqflite: ^2.4.2+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  # Tambahkan baris ini untuk mendaftarkan folder assets
  assets:
    - assets/images/
